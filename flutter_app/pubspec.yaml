name: mlm_client
description: Flutter client for MLM backend (Render-hosted)
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.5.0+1
  flutter_riverpod: ^2.5.1
  flutter_secure_storage: ^9.2.2
  intl: ^0.19.0
  collection: ^1.18.0
  url_launcher: ^6.3.0
  razorpay_flutter: ^1.3.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0

flutter:
  uses-material-design: true
